# Visual fixture

`content/posts/reading-sample.md` covers the reading surfaces that should be checked after template or style changes. It is test-only content and is never copied into the production `content/` directory.
